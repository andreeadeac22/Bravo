class Preprocessor:
    "A class the preprocesses raw data into forms that are useful,"
    
    def read_images(filenames):
        "Reads images from different formats into numpy arrays."
    
    def read_video(filename):
        "Reads a video and converts it to a series of images."
    
    def read_location(filenames):
        "Reads location data that can be used for geo-location."
        
    def read_audio(filnames):
        "Reads raw audio files."
